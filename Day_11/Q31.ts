//  No users: To make sure that the user list is not blank, you need to include an if test in exercise 28.

let users : string[] = [];

// 1.  If you don't see any entries, run the message we need to find some users! 
// 2.  Make sure that all the usernames are deleted from your list, and print a message properly.

if(users == users){
	console.log("We need to find some users!");
}

else{
	// none
}
